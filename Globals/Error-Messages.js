
const emailError = 'email already exists';
const emailNotError = 'user not found ';

const passwordError = 'please enter password';
const passwordWrongError = 'wrong password'; 

const requiredError = 'please check required fields';
const connectionError = 'connection error';

const successMessage = ' success';
const pageNotFoundError = 'page not found';

const mongoUniqueError = "Already Exists";
const castError = "Invalid Prameters or URL";
const ValidationError = "Invalid Data";


const unAuthorizedError = 'you must login';
const noPermissionError = 'you have no permissions';


module.exports = {
    emailError : emailError,
    emailNotError: emailNotError,
    passwordError: passwordError,
    passwordWrongError: passwordWrongError,
    requiredError: requiredError,
    connectionError: connectionError,
    successMessage: successMessage,
    pageNotFoundError: pageNotFoundError,
    mongoUniqueError: mongoUniqueError,
    castError: castError,
    ValidationError: ValidationError,
    unAuthorizedError: unAuthorizedError,
    noPermissionError: noPermissionError
}
